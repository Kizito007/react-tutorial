import React from "react";

function Conditional (props) { 
    return (
        <div>

            {/* {//condition ? statement is true : statment is false
            props.isLoading ? <h1>Wait a Minute </h1> : <h1> This statement {"is " + props.isLoading} </h1>} */}
            <h1>Some cool stuff about conditional rendering...</h1> 
        </div> 
    )
    
}

export default Conditional;